{
  "name": "lorerelay",
  "displayName": "LoreRelay",
  "description": "Local-first AI Game Master UI. Works with any AI assistant.",
  "version": "1.6.1",
  "publisher": "Miya",
  "author": "Miya",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "license": "MIT",
  "keywords": [
    "ai",
    "game-master",
    "roleplay",
    "trpg",
    "visual-novel",
    "sillytavern",
    "grok",
    "llm"
  ],
  "activationEvents": [
    "onCommand:textadventure.openGame",
    "onCommand:textadventure.listImageModels",
    "onCommand:textadventure.loadScenario",
    "onCommand:textadventure.importStCharacter",
    "onCommand:textadventure.importStLorebook",
    "onCommand:textadventure.exportScenario",
    "onCommand:textadventure.validateScenario",
    "onCommand:textadventure.setOpenRouterApiKey",
    "onCommand:textadventure.clearOpenRouterApiKey",
    "onCommand:textadventure.checkForUpdates",
    "onCommand:textadventure.startRemotePlay",
    "onCommand:textadventure.stopRemotePlay",
    "onCommand:textadventure.rotateRemotePlayToken"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "textadventure.openGame",
        "title": "LoreRelay: Open Game UI"
      },
      {
        "command": "textadventure.listImageModels",
        "title": "LoreRelay: List Image Models"
      },
      {
        "command": "textadventure.loadScenario",
        "title": "LoreRelay: Load Scenario Pack"
      },
      {
        "command": "textadventure.importStCharacter",
        "title": "LoreRelay: Import SillyTavern Character Card"
      },
      {
        "command": "textadventure.importStLorebook",
        "title": "LoreRelay: Import SillyTavern Lorebook"
      },
      {
        "command": "textadventure.exportScenario",
        "title": "LoreRelay: Export Scenario Pack (Workshop ZIP)"
      },
      {
        "command": "textadventure.validateScenario",
        "title": "LoreRelay: Validate Scenario Pack"
      },
      {
        "command": "textadventure.setOpenRouterApiKey",
        "title": "LoreRelay: Set OpenRouter API Key"
      },
      {
        "command": "textadventure.clearOpenRouterApiKey",
        "title": "LoreRelay: Clear OpenRouter API Key"
      },
      {
        "command": "textadventure.checkForUpdates",
        "title": "LoreRelay: Check for Updates"
      },
      {
        "command": "textadventure.startRemotePlay",
        "title": "LoreRelay: Start Remote Play (LAN)"
      },
      {
        "command": "textadventure.stopRemotePlay",
        "title": "LoreRelay: Stop Remote Play"
      },
      {
        "command": "textadventure.rotateRemotePlayToken",
        "title": "LoreRelay: Rotate Remote Play Token"
      },
      {
        "command": "textadventure.generateWorldForge",
        "title": "LoreRelay: Generate World Forge"
      }
    ],
    "configuration": {
      "title": "LoreRelay",
      "properties": {
        "textAdventure.locale": {
          "type": "string",
          "enum": [
            "ja",
            "en",
            "zh-CN",
            "zh-TW"
          ],
          "enumDescriptions": [
            "日本語",
            "English",
            "简体中文 (Simplified Chinese)",
            "繁體中文 (Traditional Chinese / Taiwan)"
          ],
          "default": "en",
          "description": "UI language for Webview, extension messages, and GM narrative prompts. Also changeable from the Webview header dropdown."
        },
        "textAdventure.skillPath": {
          "type": "string",
          "default": "C:\\AI\\TextAdventureGMSkill\\scripts\\comfyui_generate.py",
          "description": "Absolute path to the comfyui_generate.py script"
        },
        "textAdventure.imageGen.backend": {
          "type": "string",
          "enum": [
            "comfyui",
            "stabilitymatrix",
            "custom"
          ],
          "enumDescriptions": [
            "Standalone ComfyUI (default 127.0.0.1:8188)",
            "ComfyUI bundled in Stability Matrix",
            "Any ComfyUI-compatible server (set the URL manually)"
          ],
          "default": "comfyui",
          "description": "Image generation backend. All options speak the ComfyUI API; this is mostly a label — the actual endpoint is set by imageGen.comfyuiUrl."
        },
        "textAdventure.imageGen.comfyuiUrl": {
          "type": "string",
          "default": "http://127.0.0.1:8188",
          "description": "ComfyUI / Stability Matrix server URL. Change the port here if your server is not on 8188."
        },
        "textAdventure.imageGen.checkpoint": {
          "type": "string",
          "default": "",
          "description": "Checkpoint (.safetensors) name to use, exactly as shown by 'LoreRelay: List Image Models' — it may include a subfolder prefix, e.g. 'IL\\prefectIllustriousXL_v8.safetensors'. Leave empty to use the workflow default."
        },
        "textAdventure.imageGen.workflowPath": {
          "type": "string",
          "default": "",
          "description": "Optional path to a custom ComfyUI API-format workflow JSON. Leave empty to use the bundled workflow_api.json."
        },
        "textAdventure.imageGen.steps": {
          "type": "number",
          "default": 0,
          "description": "Sampling steps override (0 = use workflow default, typically 20)."
        },
        "textAdventure.imageGen.cfg": {
          "type": "number",
          "default": 0,
          "description": "CFG scale override (0 = use workflow default, typically 7)."
        },
        "textAdventure.imageGen.width": {
          "type": "number",
          "default": 0,
          "description": "Image width override in pixels (0 = use workflow default, typically 512)."
        },
        "textAdventure.imageGen.height": {
          "type": "number",
          "default": 0,
          "description": "Image height override in pixels (0 = use workflow default, typically 512)."
        },
        "textAdventure.imageGen.autoOnLocationChange": {
          "type": "boolean",
          "default": false,
          "description": "Automatically generate a scene image when the player's location changes in the World Forge."
        },
        "textAdventure.imageGen.includeFactionInPrompt": {
          "type": "boolean",
          "default": true,
          "description": "Include the controlling faction's atmosphere in the generated location image prompt."
        },
        "textAdventure.imageGen.includeDangerInPrompt": {
          "type": "boolean",
          "default": true,
          "description": "Include the region's danger level atmosphere in the generated location image prompt."
        },
        "textAdventure.vlm.provider": {
          "type": "string",
          "enum": [
            "disabled",
            "ollama",
            "openrouter",
            "openai",
            "gemini",
            "anthropic"
          ],
          "default": "disabled",
          "description": "Select the VLM provider to analyze generated images (Soulgaze)."
        },
        "textAdventure.vlm.model": {
          "type": "string",
          "default": "llava",
          "description": "VLM Model name (e.g., 'llava' for Ollama, 'gpt-4o-mini' for OpenAI, 'gemini-1.5-flash' for Gemini, 'claude-3-5-sonnet-latest' for Anthropic)."
        },
        "textAdventure.vlm.endpoint": {
          "type": "string",
          "default": "http://localhost:11434",
          "description": "Endpoint URL for the VLM provider (used for Ollama or custom local endpoints)."
        },
        "textAdventure.bgm.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable automatic background music. The GM picks a track via the 'bgm' or 'mood' field in game_state.json."
        },
        "textAdventure.bgm.manifestPath": {
          "type": "string",
          "default": "",
          "description": "Path to the BGM manifest JSON. Leave empty to use 'bgm.json' in the workspace root. Audio files are resolved relative to the manifest folder (and its 'bgm/' subfolder)."
        },
        "textAdventure.bgm.volume": {
          "type": "number",
          "default": 50,
          "description": "Default BGM volume (0-100). Can be adjusted live with the slider in the UI."
        },
        "textAdventure.sfx.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable one-shot sound effects. The GM triggers them via the 'sfx' field in game_state.json. Bundled license-free SFX work out of the box."
        },
        "textAdventure.sfx.manifestPath": {
          "type": "string",
          "default": "",
          "description": "Path to the SFX manifest JSON. Leave empty to use 'sfx.json' in the workspace, falling back to the bundled skill sfx.json. Audio files resolve relative to the manifest folder (and its 'sfx/' subfolder)."
        },
        "textAdventure.sfx.volume": {
          "type": "number",
          "default": 70,
          "description": "Default sound-effect volume (0-100)."
        },
        "textAdventure.mediaAgent.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable the background MediaAgent pipeline (early BGM/SFX from GM stream, queued ComfyUI image generation)."
        },
        "textAdventure.mediaAgent.autoImage": {
          "type": "boolean",
          "default": true,
          "description": "When a GM entry has imagePrompt but no image, queue ComfyUI generation in the background without blocking the next turn."
        },
        "textAdventure.mediaAgent.maxImageQueue": {
          "type": "number",
          "default": 5,
          "minimum": 1,
          "maximum": 20,
          "description": "Maximum queued ComfyUI image jobs. Oldest jobs are dropped when full."
        },
        "textAdventure.remotePlay.port": {
          "type": "number",
          "default": 9473,
          "minimum": 1024,
          "maximum": 65535,
          "description": "TCP port for the LAN remote player HTTP/WebSocket server."
        },
        "textAdventure.remotePlay.bindAddress": {
          "type": "string",
          "default": "127.0.0.1",
          "description": "Bind address for remote play. Use 0.0.0.0 for LAN/Tailscale access (warning: exposes server to network), or 127.0.0.1 for local-only."
        },
        "textAdventure.remotePlay.maxClients": {
          "type": "number",
          "default": 8,
          "minimum": 1,
          "maximum": 32,
          "description": "Maximum simultaneous WebSocket player connections."
        },
        "textAdventure.remotePlay.inputCooldownMs": {
          "type": "number",
          "default": 1500,
          "minimum": 500,
          "maximum": 10000,
          "description": "Minimum milliseconds between remote player inputs per client."
        },
        "textAdventure.remotePlay.defaultRole": {
          "type": "string",
          "enum": [
            "player",
            "spectator"
          ],
          "default": "player",
          "description": "Default role for remote clients that omit ?role= or auth.role."
        },
        "textAdventure.workspaceFolder": {
          "type": "string",
          "default": "",
          "description": "When multiple workspace folders are open, use this folder name or path for game_state.json. Empty = first folder."
        },
        "textAdventure.gitAutoCommitInterval": {
          "type": "number",
          "default": 1,
          "description": "Interval of turns to auto-commit history to Git. Set to 0 to disable automatic Git commits."
        },
        "textAdventure.gmBridge.provider": {
          "type": "string",
          "enum": [
            "grok",
            "clipboard",
            "command",
            "ollama",
            "koboldcpp",
            "openrouter"
          ],
          "default": "grok",
          "enumDescriptions": [
            "Grok Build headless (grok -p)",
            "Copy player input to clipboard only",
            "Custom executable via gmBridge.command",
            "Local Ollama via ollama_gm.py (ollama serve required)",
            "Local KoboldCPP via koboldcpp_gm.py (koboldcpp.exe API required)",
            "OpenRouter via openrouter_gm.py (Cloud-based API, requires API Key)"
          ],
          "description": "How player input from the Webview is sent to the GM."
        },
        "textAdventure.gmBridge.python": {
          "type": "string",
          "default": "python",
          "description": "Python executable for ollama/koboldcpp GM bridge scripts."
        },
        "textAdventure.gmBridge.scriptPath": {
          "type": "string",
          "default": "",
          "description": "Optional path to any GM bridge script (e.g. ollama_gm.py). Sibling scripts in the same folder are resolved automatically."
        },
        "textAdventure.gmBridge.ollama.url": {
          "type": "string",
          "default": "http://localhost:11434",
          "description": "Ollama API base URL (used when gmBridge.provider=ollama)."
        },
        "textAdventure.gmBridge.ollama.model": {
          "type": "string",
          "default": "llama3.2",
          "description": "Ollama model name (e.g. llama3.2, gemma3, qwen2.5)."
        },
        "textAdventure.gmBridge.koboldcpp.url": {
          "type": "string",
          "default": "http://127.0.0.1:5001",
          "description": "KoboldCPP API base URL (used when gmBridge.provider=koboldcpp)."
        },
        "textAdventure.gmBridge.openRouter.apiKey": {
          "type": "string",
          "default": "",
          "description": "Deprecated fallback OpenRouter API key. Prefer the LoreRelay: Set OpenRouter API Key command, which stores the key in VS Code SecretStorage."
        },
        "textAdventure.gmBridge.openRouter.model": {
          "type": "string",
          "default": "anthropic/claude-3.5-sonnet",
          "description": "OpenRouter model name to use (e.g. anthropic/claude-3.5-sonnet, google/gemini-1.5-pro, openai/gpt-4o)."
        },
        "textAdventure.gmBridge.command": {
          "type": "string",
          "default": "",
          "description": "When gmBridge.provider=command, path to the executable (e.g. grok, or another CLI)."
        },
        "textAdventure.gmBridge.commandArgs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "--prompt-file",
            "{actionFile}",
            "--cwd",
            "{cwd}",
            "--always-approve"
          ],
          "description": "Arguments for custom GM command. Placeholders: {actionFile} (recommended), {action}, {cwd}."
        },
        "textAdventure.grokBridge.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Automatically send player input from Webview to Grok Build (headless mode)"
        },
        "textAdventure.grokBridge.command": {
          "type": "string",
          "default": "grok",
          "description": "Path to grok CLI (auto-detects ~/.grok/bin/grok.exe on Windows when set to 'grok')"
        },
        "textAdventure.grokBridge.autoApprove": {
          "type": "boolean",
          "default": false,
          "description": "Pass --always-approve to grok for non-interactive tool approval. Warning: enabling this allows automated execution without prompt verification."
        },
        "textAdventure.grokBridge.fallbackToClipboard": {
          "type": "boolean",
          "default": true,
          "description": "Copy player input to clipboard when Grok bridge is disabled or fails"
        },
        "textAdventure.archive.autoPrompt": {
          "type": "boolean",
          "default": true,
          "description": "Suggest archiving old history turns into saga chapters when the log grows long."
        },
        "textAdventure.archive.thresholdSmallContext": {
          "type": "number",
          "default": 30,
          "description": "History turn count to suggest archive for Ollama / KoboldCPP / small OpenRouter models."
        },
        "textAdventure.archive.thresholdLargeContext": {
          "type": "number",
          "default": 80,
          "description": "History turn count to suggest archive for Grok / Gemini-class large-context models."
        },
        "textAdventure.archive.remindEvery": {
          "type": "number",
          "default": 15,
          "description": "After the threshold, remind every N additional turns."
        },
        "textAdventure.worldForge.defaultRegionCount": {
          "type": "number",
          "default": 5,
          "minimum": 3,
          "maximum": 12,
          "description": "Default number of regions when generating a new World Forge."
        },
        "textAdventure.worldForge.defaultFactionCount": {
          "type": "number",
          "default": 3,
          "minimum": 2,
          "maximum": 6,
          "description": "Default number of factions when generating a new World Forge."
        },
        "textAdventure.worldForge.defaultNpcCount": {
          "type": "number",
          "default": 6,
          "minimum": 2,
          "maximum": 20,
          "description": "Default number of initial NPCs when generating a new World Forge."
        },
        "textAdventure.worldForge.llmEnrich": {
          "type": "boolean",
          "default": false,
          "description": "When enabled, LLM is used to enrich generated world descriptions (experimental)."
        },
        "textAdventure.memory.backend": {
          "type": "string",
          "enum": [
            "auto",
            "tfidf",
            "chromadb"
          ],
          "enumDescriptions": [
            "Use ChromaDB if pip install chromadb succeeded, else TF-IDF",
            "Always use lightweight TF-IDF (no extra deps)",
            "Require ChromaDB embedding search (falls back if not installed)"
          ],
          "default": "auto",
          "description": "Memory Bank search backend for GM prompt injection."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "build:webview": "node scripts/build-webview.js",
    "compile": "npm run build:webview && tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "node scripts/validate.js && node scripts/test_media_paths.js && node scripts/test_lorebook_save.js && node scripts/test_scenario_director.js && node scripts/test_party_director.js && node scripts/test_sample_scenarios.js && node scripts/test_memory_bank.js && node scripts/test_migrate_game_state.js && node scripts/test_remote_play_server.js && node scripts/test_ws_functionality.js && node scripts/test_validate_game_state.js && node scripts/test_dice_roller.js && node scripts/test_gm_prompt_builder_core.js && node scripts/test_webview_handlers_core.js && node scripts/test_webview_bundle.js && node scripts/test_world_forge.js && node scripts/test_world_state.js && node scripts/test_emergent_simulator.js && node scripts/test_npc_registry.js && node scripts/test_world_map_generator.js && node scripts/test_world_forge_generator.js && node scripts/test_location_image_builder.js && node scripts/test_world_event_log.js && node scripts/test_npc_bridge.js && node scripts/test_visual_memory.js && node scripts/test_vlm_queue_core.js && node scripts/test_tavern_card_importer.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "mermaid": "^11.16.0",
    "ws": "^8.21.0"
  }
}
